#include<stdio.h>
int main() {
	
	int a, b, c, d;
	
	printf("Please enter any four numbers: ");
	scanf("%d %d %d %d", &a, &b, &c, &d);
	
	if (c == d) {
		printf("We can not to find ratio.");
	} else 
	
	return;
}
